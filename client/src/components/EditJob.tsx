import React, { FC } from 'react'
import { Job } from '../features/jobs/jobModel'

interface EditJobProps {
    job: Job
}

const EditJob:FC<EditJobProps> = ({job}) => {
  return (
    <div>
        
    </div>
  )
}

export default EditJob